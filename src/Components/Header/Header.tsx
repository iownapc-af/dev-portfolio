import './Header.scss';
import { HeaderNav } from './HeaderNav/HeaderNav';

const Header = () => {
  return (
    <header>
      {/* <div className="header-logo">LOGO</div> */}
      <HeaderNav />
    </header>
  );
};

export default Header;
